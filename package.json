{
  "name": "contract-tester",
  "version": "0.0.1",
  "description": "Simple tool for validating if service is meeting the contract",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Stefan Szarek",
  "license": "ISC",
  "dependencies": {
    "request": "^2.83.0",
    "request-promise": "^4.2.2"
  }
}
